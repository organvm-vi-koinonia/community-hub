services:
  - type: web
    name: community-hub
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ALLOWED_ORIGINS
        value: ""
      - key: PORT
        value: 8000
    plan: free
    region: oregon
